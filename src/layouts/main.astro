---
import '../styles/global.css';
import { Header } from '@/components/Header';
import { Footer } from '@/components/Footer';
import { CommandPalette } from '@/components/CommandPalette';
import { CookieConsent } from '@/components/CookieConsent';
const { title = "Paradigm Solutions - Enterprise Communications Infrastructure" } = Astro.props;
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="description" content="A modern, free Astro theme template for Paradigm Solutions startups with dark/light mode toggle" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<title>{title}</title>
	</head>
	<body>
		<Header client:only="react" />
		<main>
			<slot />
		</main>
		<Footer />
		<CommandPalette client:only="react" />
		<CookieConsent client:only="react" />
	</body>
</html>
